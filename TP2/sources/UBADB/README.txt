Tests
=====
1) All components are implemented using interfaces to allow the creation of mocks for tests
2) Some methods that should be private are defined as protected so that they can be tested with JUnit
